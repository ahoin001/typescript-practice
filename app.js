// * NOTE TYPESCRIPT objects are key->type pairs, not key -> value pairs
// const person: {
//     name: string;
//     age: number;
// } = {
var person = {
    name: 'Alexander',
    age: 24
};
console.log(person.name);
